interface:
  display_name: "Add Kitchen Appliance"
  short_description: "Download manual + photos, tabulate functions and specs, update inventory"
  default_prompt: "Use $add-kitchen-appliance to add this appliance: download its manual and photos, extract its functions/programs and parameters into the inventory tables."
policy:
  allow_implicit_invocation: true
